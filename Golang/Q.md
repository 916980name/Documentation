### params copy
1. 1 int param
    ```
    // a = 10
    func ptest2(a int) int
    ```
    The param stored in register `$rax`
1. 3 int param

1. 1 string param

1. 1 pointer param

1. 1 struct param